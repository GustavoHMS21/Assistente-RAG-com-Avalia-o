# Copie este arquivo como .env e preencha somente no seu computador.
# O projeto usa o Ollama rodando localmente, então nenhuma chave de API é necessária.

# Endereço do servidor Ollama local (opcional; padrão já é este mesmo valor).
OLLAMA_HOST=http://localhost:11434
